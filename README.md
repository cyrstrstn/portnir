# PortGuard

Windows listening-ports tool (CLI + Tauri desktop planned).

## Workspace

- `crates/portguard-core` — shared library
- `crates/portguard-cli` — `portguard` CLI binary

## Build

```powershell
cargo check
```
