[package]
name = "portguard-cli"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "portguard"
path = "src/main.rs"

[dependencies]
portguard-core = { path = "../portguard-core" }
clap = { version = "4", features = ["derive"] }
serde_json = "1"
